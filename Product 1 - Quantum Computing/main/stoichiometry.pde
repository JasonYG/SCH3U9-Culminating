void stoichiometry() {
  background(0);
  fill(255);

  textAlign(CENTER);
  textSize(28);
  text("Creating a quantum chip of silicon carbide", width/2, 50);
  
  textAlign(LEFT);
  textSize(18);
  text("The following is a stoichiometric calculation to create a 5 mm by 5 mm by 1 mm quantum \nchip of silicon carbide.", 50, 100);
}