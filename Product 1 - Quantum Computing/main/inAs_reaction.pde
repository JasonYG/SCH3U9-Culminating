void inAs_reaction() {  
  background(0);
  fill(255);

  textAlign(CENTER);
  textSize(28);
  text("Creating InAs", width/2, 50);
  
  textAlign(LEFT);
  textSize(18);
  text("The following chemical reaction can occur to produce indium arsenide:", 50, 100);
  
  textAlign(CENTER);
  textSize(28);
  text("In + As --> InAs", width/2, 150);
  
  textAlign(LEFT);
  textSize(18);
  text("This is a typical synthesis chemical reaction that can produces InAs.", 50, 200);
  
  resetSettings();
}