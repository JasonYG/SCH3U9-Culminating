void intro_screen() {
  background(255);
  imageMode(CENTER);
  image(intro_text, width/2, height/2, width, height);
}

void background_info() {
  background(255);
  imageMode(CENTER);
  image(background_text, width/2, height/2, width, height);
}

void hydrocarbon_info() {
  background(255);
  imageMode(CENTER);
  image(hydrocarbon_text, width/2, height/2, width, height);
}

void hydrocarbon_table() {
  background(255);
  imageMode(CENTER);
  image(hydrocarbon_naming, width/2, height/2, width, height);
}

void alcohol_properties() {
  background(255);
  imageMode(CENTER);
  image(alcohol_properties_text, width/2, height/2, width, height);
}
void carboxylic_acids() {
  background(255);
  imageMode(CENTER);
  image(carboxylic_acids_text, width/2, height/2, width, height);
}
void carboxylic_properties() {
  background(255);
  imageMode(CENTER);
  image(carboxylic_properties_text, width/2, height/2, width, height);
}
void periodic_trends() {
  background(255);
  imageMode(CENTER);
  image(periodic_trends_text, width/2, height/2, width, height);
}
void more_trends() {
  background(255);
  imageMode(CENTER);
  image(more_trends_text, width/2, height/2, width, height);
}
void relevance_to_orgo() {
  background(255);
  imageMode(CENTER);
  image(relevance_to_orgo, width/2, height/2, width, height);
}
void amino_acids() {
  background(255);
  imageMode(CENTER);
  image(amino_acids_text, width/2, height/2, width, height);
}
void bio_reactions() {
  background(255);
  imageMode(CENTER);
  image(bio_reactions_text, width/2, height/2, width, height);
}
void condensation() {
  background(255);
  imageMode(CENTER);
  image(condensation_text, width/2, height/2, width, height);
}
void example_reaction() {
  background(255);
  imageMode(CENTER);
  image(example_reaction_text, width/2, height/2, width, height);
}
void energy_change() {
  background(255);
  imageMode(CENTER);
  image(energy_change_text, width/2, height/2, width, height);
}
void redox() {
  background(255);
  imageMode(CENTER);
  image(redox_text, width/2, height/2, width, height);
}
void redox_reaction() {
  background(255);
  imageMode(CENTER);
  image(redox_reaction_text, width/2, height/2, width, height);
}
void stoich() {
  background(255);
  imageMode(CENTER);
  image(stoich_text, width/2, height/2, width, height);
}
void stoich_continued () {
  background(255);
  imageMode(CENTER);
  image(stoich_continued, width/2, height/2, width, height);
}
void organic_acid_bases() {
  background(255);
  imageMode(CENTER);
  image(organic_acid_bases_text, width/2, height/2, width, height);
}
void pka() {
  background(255);
  imageMode(CENTER);
  image(pka_text, width/2, height/2, width, height);
}
void pka_ph() {
  background(255);
  imageMode(CENTER);
  image(pka_ph_text, width/2, height/2, width, height);
}
void organic_base() {
  background(255);
  imageMode(CENTER);
  image(organic_base_text, width/2, height/2, width, height);
}
void acidity_of_carboxylic_acids() {
  background(255);
  imageMode(CENTER);
  image(acidity_of_carboxylic_acids_text, width/2, height/2, width, height);
}
void career() {
  background(255);
  imageMode(CENTER);
  image(career_text, width/2, height/2, width, height);
}