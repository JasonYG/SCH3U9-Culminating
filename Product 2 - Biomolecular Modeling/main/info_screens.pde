void intro_screen() {
  background(255);
  imageMode(CENTER);
  image(intro_text, width/2, height/2, width, height);
}

void background_info() {
  background(255);
  imageMode(CENTER);
  image(background_text, width/2, height/2, width, height);
}

void hydrocarbon_info() {
  background(255);
  imageMode(CENTER);
  image(hydrocarbon_text, width/2, height/2, width, height);
}

void hydrocarbon_table() {
  background(255);
  imageMode(CENTER);
  image(hydrocarbon_naming, width/2, height/2, width, height);
}