void intro_screen() {
  background(255);
  imageMode(CENTER);
  image(intro_text, width/2, height/2, width, height);
}

void how_MD_works() {
  background(255);
  imageMode(CENTER);
  image(how_MD_works_text, width/2, height/2, width, height);
}

void applications() {
  background(255);
  imageMode(CENTER);
  image(applications_text, width/2, height/2, width, height);
}

void applications_cont() {
  background(255);
  imageMode(CENTER);
  image(applications_cont_text, width/2, height/2, width, height);
}

void kmt() {
  background(255);
  imageMode(CENTER);
  image(kmt_text, width/2, height/2, width, height);
}

void ideal_gas_screen() {
  background(255);
  imageMode(CENTER);
  image(ideal_gas_text, width/2, height/2, width, height);
}

void non_ideal_gas() {
  background(255);
  imageMode(CENTER);
  image(non_ideal_gas_text, width/2, height/2, width, height);
}

void vdw() {
  background(255);
  imageMode(CENTER);
  image(vdw_equation_text, width/2, height/2, width, height);
}

void intermolecular_forces() {
  background(255);
  imageMode(CENTER);
  image(intermolecular_text, width/2, height/2, width, height);
}

void sa_polarizability() {
  background(255);
  imageMode(CENTER);
  image(sa_polarizability_text, width/2, height/2, width, height);
}

void simulation_explanation() {
  background(255);
  imageMode(CENTER);
  image(ideal_vs_non_ideal_text, width/2, height/2, width, height);
}
void intermolecular_protein() {
  background(255);
  imageMode(CENTER);
  image(imt_protein_text, width/2, height/2, width, height);
}
void periodic_trends() {
  background(255);
  imageMode(CENTER);
  image(periodic_trends_text, width/2, height/2, width, height);
}
void gas_evolution() {
  background(255);
  imageMode(CENTER);
  image(gas_evolution_text, width/2, height/2, width, height);
}
void gas_stoich() {
  background(255);
  imageMode(CENTER);
  image(gas_stoich_text, width/2, height/2, width, height);
}
void stoich_continued() {
  background(255);
  imageMode(CENTER);
  image(stoich_continued_text, width/2, height/2, width, height);
}
void gaseous_reaction() {
  background(255);
  imageMode(CENTER);
  image(gaseous_reaction_text, width/2, height/2, width, height);
}
void covalent_properties() {
  background(255);
  imageMode(CENTER);
  image(covalent_properties_text, width/2, height/2, width, height);
}
void collision_theory() {
  background(255);
  imageMode(CENTER);
  image(collision_theory_text, width/2, height/2, width, height);
}
void collisions_and_md() {
  background(255);
  imageMode(CENTER);
  image(collision_and_md_text, width/2, height/2, width, height);
}
void gas_simulation() {
  background(255);
  imageMode(CENTER);
  image(gas_simulation_text, width/2, height/2, width, height);
}
void acid_base() {
  background(255);
  imageMode(CENTER);
  image(acid_base_text, width/2, height/2, width, height);
}
void acid_explanation() {
  background(255);
  imageMode(CENTER);
  image(acid_explanation_text, width/2, height/2, width, height);
}
void md_relevance() {
  background(255);
  imageMode(CENTER);
  image(md_relevance_text, width/2, height/2, width, height);
}
void math() {
  background(255);
  imageMode(CENTER);
  image(math_text, width/2, height/2, width, height);
}
void more_math() {
  background(255);
  imageMode(CENTER);
  image(more_math_text, width/2, height/2, width, height);
}
void math_continued() {
  background(255);
  imageMode(CENTER);
  image(math_continued_text, width/2, height/2, width, height);
}
void career() {
  background(255);
  imageMode(CENTER);
  image(career_text, width/2, height/2, width, height);
}