void intro_screen() {
  background(255);
  imageMode(CENTER);
  image(intro_text, width/2, height/2, width, height);
}

void how_MD_works() {
  background(255);
  imageMode(CENTER);
  image(how_MD_works_text, width/2, height/2, width, height);
}

void applications() {
  background(255);
  imageMode(CENTER);
  image(applications_text, width/2, height/2, width, height);
}

void applications_cont() {
  background(255);
  imageMode(CENTER);
  image(applications_cont_text, width/2, height/2, width, height);
}

void kmt() {
  background(255);
  imageMode(CENTER);
  image(kmt_text, width/2, height/2, width, height);
}

void ideal_gas_screen() {
  background(255);
  imageMode(CENTER);
  image(ideal_gas_text, width/2, height/2, width, height);
}

void non_ideal_gas() {
  background(255);
  imageMode(CENTER);
  image(non_ideal_gas_text, width/2, height/2, width, height);
}

void vdw() {
  background(255);
  imageMode(CENTER);
  image(vdw_equation_text, width/2, height/2, width, height);
}

void intermolecular_forces() {
  background(255);
  imageMode(CENTER);
  image(intermolecular_text, width/2, height/2, width, height);
}

void sa_polarizability() {
  background(255);
  imageMode(CENTER);
  image(sa_polarizability_text, width/2, height/2, width, height);
}